#include <bits/stdc++.h>
using namespace std;

bool visited[MAXN];
void bfs(int root){
    memset(visited, 0, sizeof visited);
    queue<int> q;
    visited[root] = 1;
    q.push(root);
    while(!q.empty()){
        //find adjacents
            //if an adjacent not visted
                //mark it as visited and push it to queue;
    }
}

int main(){

}
